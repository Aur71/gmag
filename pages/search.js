const Search = () => {
  // render all the products using the serverside props
  // also add the filtering sidebar, pagination & general filters
  return <div>search</div>;
};

export default Search;
